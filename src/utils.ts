export const $=<T extends Element>(s:string,root:ParentNode=document)=>root.querySelector<T>(s);
export function download(blob:Blob,name:string){const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}
export const escapeHtml=(s:string)=>s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));
export function parsePageExpression(input:string,total:number){if(!input.trim())throw Error('Enter pages such as 3-5,8,11-15.');const pages:number[]=[];for(const part of input.split(',')){if(!/^\d+(?:-\d+)?$/.test(part))throw Error(`Invalid page segment “${part}”.`);const values=part.split('-').map(Number),a=values[0],b=values[1]??a;if(a<1||b<1||a>total||b>total||b<a)throw Error(`Pages must be between 1 and ${total}; ranges must go forward.`);for(let n=a;n<=b;n++)pages.push(n);}return [...new Set(pages)];}
export function csvRows(text:string,delimiter=','){const rows:string[][]=[];let row:string[]=[],cell='',quoted=false;for(let i=0;i<text.length;i++){const c=text[i];if(c==='"'){if(quoted&&text[i+1]==='"'){cell+='"';i++;}else quoted=!quoted;}else if(c===delimiter&&!quoted){row.push(cell);cell='';}else if((c==='\n'||c==='\r')&&!quoted){if(c==='\r'&&text[i+1]==='\n')i++;row.push(cell);if(row.some(x=>x!==''))rows.push(row);row=[];cell='';}else cell+=c;}if(quoted)throw Error('Malformed CSV: an opening quote has no closing quote.');if(cell||row.length){row.push(cell);rows.push(row);}return rows;}
export const detectDelimiter=(text:string)=>[',',';','\t'].sort((a,b)=>(text.split('\n')[0].split(b).length-text.split('\n')[0].split(a).length))[0];
export function jsonError(error:unknown,input=''){const msg=error instanceof Error?error.message:String(error);const m=msg.match(/position (\d+)/i);if(!m)return msg;const position=Number(m[1]);const before=input.slice(0,position);const line=before.split('\n').length;const column=position-(before.lastIndexOf('\n')+1)+1;return `${msg} (line ${line}, column ${column})`;}
export function jsonOperation(input:string,operation:'pretty'|'minify'|'validate'){const parsed=JSON.parse(input);return operation==='pretty'?JSON.stringify(parsed,null,2):operation==='minify'?JSON.stringify(parsed):input;}
