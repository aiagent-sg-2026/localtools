export const SAFETY={maxFileBytes:100*1024*1024,maxAggregateBytes:250*1024*1024,maxCsvBytes:50*1024*1024,maxCsvRows:250000,maxPdfPages:1000};
export const formatBytes=(n:number)=>n<1024?`${n} B`:n<1048576?`${(n/1024).toFixed(1)} KB`:`${(n/1048576).toFixed(1)} MB`;
export function validateFiles(files:File[],accept:string[],aggregate=SAFETY.maxAggregateBytes){if(!files.length) throw Error('Choose at least one file.'); if(files.some(f=>f.size>SAFETY.maxFileBytes)) throw Error(`Files must be smaller than ${formatBytes(SAFETY.maxFileBytes)}.`); if(files.reduce((a,f)=>a+f.size,0)>aggregate) throw Error('The selected files exceed the safe processing limit.'); if(files.some(f=>!accept.some(type=>f.type===type||(!f.type&&f.name.toLowerCase().endsWith(type.split('/')[1]))))) throw Error(`Supported formats: ${accept.map(x=>x.split('/')[1].toUpperCase()).join(', ')}.`);}
